const names = ["shahzad", "hamza", "ali", "qamar","toqeer", "usman", "good"];
const newNames = ["Aisha",  "Ali",  "Ibrahim",  "Fatima",  "Muhammad",  "Omar",  "Maryam",  "Nura",  "Yusuf",  "Zahra"];
let processData = function(x) {
  // const names1=x;
  
  const names2 = [];
  var newname = '';
  for (let i=0; i< x.length; i++) {
    const names3 = x[i];
    if (names3.includes("a")) {
    names2.push(names3.replace("a","@"));
            if (names2.length > 5) {
            newname = names2.slice(0 ,5);
            
            }
    } else {
      names2.push(names3.toUpperCase());
    }
    
  }
  console.log("My Array");
  console.log(x);
  console.log("Array.Slice");
  console.log(newname);
  console.log("Array.toUpperCase");
  console.log(names2);
    return names2;
  }
  
   processData(newNames);
   processData(names);

  
  
  